/**
 * Response types
 * @author Yuen Ler Chow
 */
enum ResponseType {
  Text = 'Text',
  Blob = 'Blob',
  ArrayBuffer = 'ArrayBuffer',
  FormData = 'FormData',
  JSON = 'JSON',
}

export default ResponseType;
